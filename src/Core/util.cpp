#include "util.hpp"

// template <class T>
// T nengine::util::vector::merge(T * a, T * b) {
	// T result(a->size() + b->size());
	// result->insert(result->end(), a->start(), a->end());
	// result->insert(result->end(), b->start(), b->end());
	// // result.
	// return result;
// }

// template <class T>
// std::vector<T> * nengine::util::vector::merge(
		  // std::vector<T> * a
		// , std::vector<std::vector<T>*>* vectors
	// ) {
	// uint total_size {a->size()};
	// // for(T single_vector : vectors)
		// // total_size += single_vector.size();
		//
	// std::vector<T*>* result(total_size);
	// // for(T single_vector : vectors)
		// // result->insert(result->end()
			// // , single_vector->begin()
			// // , single_vector->end());
//
	// return result;
// }

